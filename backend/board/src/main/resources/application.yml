server:
  port: ${BOARD_PORT:}

spring:
  application:
    name: ozz-board
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  data:
    elasticsearch:
      repositories:
        enabled: true

logging:
  level:
    root: info

